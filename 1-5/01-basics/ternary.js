condition ? valueIfTrue : valueIfFalse;
//is condition true? → if yes, the part after ? → if no, the part after :

// if/else
if (age >= 18) {
  return "adult";
} else {
  return "minor";
}

// same thing, ternary
return age >= 18 ? "adult" : "minor";
