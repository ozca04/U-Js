/* ============================================================
   NESTING
   Objects inside arrays, and arrays inside objects.
   ============================================================ */

//nest arrays in objects or nest objects in array 
const cars = [
    {brand: 'audi', year: 2005, milage:20030 },
    {brand: 'nissan', year:2002, milage:32000},
    {beand: 'bmw', year: 2012, milage:20000}
]//objects nested in array 

const studentList = {
    className: '12-B',
    classFloor: 3,
    exams:['Math', 'Chem', 'Psy'],
    students:['ahmet', 'mehmet', 'ayse', 'fatma']
}//arrays nested in an object